name: Deploy Solana Program

on:
  push:
    branches:
      - main
    paths:
      - 'programs/**'
      - 'Anchor.toml'
  workflow_dispatch:
    inputs:
      network:
        description: 'Network to deploy to'
        required: true
        default: 'devnet'
        type: choice
        options:
          - devnet
          - mainnet-beta

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust 1.79 (compatible with Anchor 0.30.1)
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.79.0

      - name: Install Solana CLI and setup PATH
        run: |
          # Try Anza URL first (new), fallback to Solana Labs URL
          curl -sSfL https://release.anza.xyz/v1.18.17/install | sh || \
          curl -sSfL https://release.solana.com/v1.18.17/install | sh
          export PATH="$HOME/.local/share/solana/install/active_release/bin:$PATH"
          echo "$HOME/.local/share/solana/install/active_release/bin" >> $GITHUB_PATH
          solana --version

      - name: Install Anchor CLI
        run: |
          cargo install --git https://github.com/coral-xyz/anchor --tag v0.30.1 anchor-cli --locked

      - name: Setup Solana wallet and configure network
        run: |
          mkdir -p ~/.config/solana
          echo '${{ secrets.SOLANA_PRIVATE_KEY }}' > ~/.config/solana/id.json
          chmod 600 ~/.config/solana/id.json
          
          NETWORK=${{ github.event.inputs.network || 'devnet' }}
          if [ "$NETWORK" = "mainnet-beta" ]; then
            solana config set --url https://api.mainnet-beta.solana.com
          else
            solana config set --url https://api.devnet.solana.com
          fi
          solana config set --keypair ~/.config/solana/id.json

      - name: Check wallet balance
        run: |
          echo "Wallet address:"
          solana address
          echo "Balance:"
          BALANCE=$(solana balance | awk '{print $1}')
          echo "$BALANCE SOL"
          if (( $(echo "$BALANCE < 3" | bc -l) )); then
            echo "ERROR: Insufficient balance. Need at least 3 SOL to deploy."
            exit 1
          fi

      - name: Setup Program Keypair
        run: |
          mkdir -p target/deploy
          echo '${{ secrets.PROGRAM_KEYPAIR }}' > target/deploy/pm15-keypair.json
          chmod 600 target/deploy/pm15-keypair.json
          PROGRAM_ID=$(solana address -k target/deploy/pm15-keypair.json)
          echo "Program ID: $PROGRAM_ID"
          echo "PROGRAM_ID=$PROGRAM_ID" >> $GITHUB_ENV

      - name: Build Anchor program
        run: |
          echo "Building Anchor program..."
          echo "Rust version: $(rustc --version)"
          
          # Remove Cargo.lock if it exists (may have incompatible lockfile version)
          rm -f Cargo.lock
          echo "Removed old Cargo.lock"
          
          # Generate fresh lockfile with current stable Rust
          cargo generate-lockfile
          echo "Generated new Cargo.lock"
          
          # Downgrade lockfile version from 4 to 3 for Anchor compatibility
          # Anchor CLI 0.30.1 uses an older Cargo that doesn't support v4
          sed -i 's/^version = 4$/version = 3/' Cargo.lock
          echo "Downgraded Cargo.lock to version 3"
          head -5 Cargo.lock
          
          anchor build 2>&1 | tee build_output.txt
          BUILD_EXIT_CODE=${PIPESTATUS[0]}
          
          echo ""
          echo "Build artifacts:"
          ls -la target/deploy/ || echo "target/deploy not found"
          
          if [ ! -f target/deploy/pm15.so ]; then
            echo ""
            echo "ERROR: Build failed - pm15.so not found"
            echo ""
            echo "Build output:"
            cat build_output.txt
            exit 1
          fi
          
          echo ""
          echo "Program binary size:"
          ls -lh target/deploy/pm15.so

      - name: Deploy to Solana
        run: |
          NETWORK=${{ github.event.inputs.network || 'devnet' }}
          echo "Deploying to $NETWORK..."
          echo "Program ID: $PROGRAM_ID"
          echo ""
          
          # Run deploy and capture output
          anchor deploy --provider.cluster $NETWORK 2>&1 | tee deploy_output.txt
          DEPLOY_EXIT_CODE=${PIPESTATUS[0]}
          
          if [ $DEPLOY_EXIT_CODE -ne 0 ]; then
            echo ""
            echo "ERROR: Deploy command failed with exit code $DEPLOY_EXIT_CODE"
            cat deploy_output.txt
            exit 1
          fi
          
          # Check if deploy actually succeeded by verifying program exists
          echo ""
          echo "Verifying deployment..."
          sleep 10
          
          # Use correct RPC URL for verification based on network
          if [ "$NETWORK" = "mainnet-beta" ]; then
            RPC_URL="https://api.mainnet-beta.solana.com"
          else
            RPC_URL="https://api.devnet.solana.com"
          fi
          
          if solana program show $PROGRAM_ID --url $RPC_URL; then
            echo ""
            echo "SUCCESS: Program verified on-chain!"
          else
            echo ""
            echo "ERROR: Program not found on-chain after deployment"
            echo "Deploy output was:"
            cat deploy_output.txt
            exit 1
          fi

      - name: Output Program ID
        run: |
          echo "=============================================="
          echo "DEPLOYMENT SUCCESSFUL"
          echo "=============================================="
          echo "Program ID: $PROGRAM_ID"
          echo "Network: ${{ github.event.inputs.network || 'devnet' }}"
          echo "Explorer: https://explorer.solana.com/address/$PROGRAM_ID?cluster=${{ github.event.inputs.network || 'devnet' }}"
          echo "=============================================="
